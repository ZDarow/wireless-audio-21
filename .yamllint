---
extends: default

rules:
  # Длинные комментарии/URL в workflow — допустимы до 120 символов.
  line-length:
    max: 120
  document-start:
    present: true
  truthy:
    # `on:` — ключевое слово GitHub Actions, не булевый литерал.
    check-keys: false
  new-line-at-end-of-file: enable
